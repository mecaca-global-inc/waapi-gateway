# Go
/bin/
/waapi-gateway
/whatsapp-gateway
*.test
*.out
coverage.out

# Storage
/storages/
*.db
*.db-journal

# Env
.env
.env.local
.env.*.local

# Node / Next
node_modules/
.next/
# Dashboard static export copied at Docker build time (keep placeholder
# so the go:embed in internal/web/web.go compiles on a fresh clone).
/internal/web/dist/*
!/internal/web/dist/.gitkeep

# Editor
.DS_Store
.idea/
.vscode/
